# homebrew-tap

Homebrew tap for [OriginCoderPulse](https://github.com/OriginCoderPulse).

## Install

```bash
brew tap OriginCoderPulse/tap
brew install nusic
```

Then run:

```bash
nusic
```

Music files go in `~/.music`.
